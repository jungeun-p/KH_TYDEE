<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="itemorder-mapper">
	<update id="insertList" parameterType="java.util.List">
		<foreach collection="list" item="item" open="INSERT ALL " close=" SELECT * FROM DUAL " separator=" ">
		INTO ITEM_ORDER
		(ORDER_NO, ORDER_TITLE, USER_NO, ADDRESS_NO, MERCHANT_UID, ITEM_NO, ITEM_QUAN, ITEM_PRICE, ITEM_TOTAL_PRICE)
		VALUES(
		ITEM_ORDER_SEQ.NEXTVAL,
		#{item.order_title},
		#{item.user_no},
		#{item.address_no},
		#{item.merchant_uid},
		#{item.item_no},
		#{item.item_quan},
		#{item.item_price},
		#{item.item_total_price},
		SYSDATE
		)
		</foreach>
	</update>
	<select id="selectList" parameterType="int">
		SELECT ORDER_NO, DISTINCT ORDER_TITLE, USER_NO, DISTINCT MERCHNAT_UID, ITEM_TOTAL_PRICE, ORDER_REGDATE
		FROM ITEM_ORDER
		WHERE USER_NO = #{user_no}
	</select>
	<select id="selectOne" parameterType="int">
		SELECT ORDER_NO, ORDER_TITLE, USER_NO, ADDRESS_NO, MERCHANT_UID, ITEM_NO, ITEM_QUAN, ITEM_PRICE, ITEM_TOTAL_PRICE, ORDER_REGDATE
		FROM ITEM_ORDER
		WHERE ORDER_NO = #{order_no}
	</select>
</mapper>
